import { Builder, Locator, ThenableWebDriver, WebElementPromise, until } from 'selenium-webdriver';
import { BasePage } from "./basepage";

const gAssert = require("assert");

export class Asserts extends BasePage {
    
}